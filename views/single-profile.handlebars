<div class="row">
  <div class="col-md-6">
    <div class="card">
      <div class="card-body">
        <div class="card col-12 ">
    <div class="card-body">
 

    <!-- image is half of col 12 left -->
    <div class="card">
        <div class="col-6">
    
      <img src="{{profile.photo}}" class="card-img-top" alt="{{profile.photo}}">
    </div>
    <br>
    </div>

</div>
        <p class="lead">{{profile.user.name}}</p>
        <p>{{profile.city}}, {{profile.country}}</p>
        <p>{{profile.email}}</p>
        <p>{{profile.phone}}</p>

      </div>
    </div>


  </div>
  <div class="col-md-6">
    <div class="card">
      <div class="card-body">
        <p>{{profile.about}}</p>
        <p>{{profile.experience}}</p>
        <p>{{profile.hobbies}}</p>
        <div class="mb-3 card ">
              <label for="facebook" class="form-label" ><img src="/img/facebook.png" height="50" width="50"/><a class="px-2" href={{profile.facebook_link}}>{{profile.facebook_link}}</a></label>

              <label for="linkedin" class="form-label"><img src="/img/linkedin.png" height="50" width="50"/><a class="px-2" href={{profile.linkedin_link}}>{{profile.linkedin_link}}</a></label>

              <label for="github" class="form-label"><img src="/img/github (1).png" height="50" width="50"/><a class="px-2" href={{profile.github_link}}>{{profile.github_link}}</a></label>
            </div>
      </div>
    </div>
  </div>
</div>